import React from 'react';
import ReactDOM from 'react-dom/client';
import Become from './become';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<React.StrictMode><Become /></React.StrictMode>);
